<UserControl x:Class="TestApp.Controls.ucMvvmCounter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <!-- DataContext는 code-behind(ucMvvmCounter.xaml.cs)에서 연결한다 -->
    <Grid Background="#FFF8E1">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="MVVM" FontSize="14" Foreground="#888888" HorizontalAlignment="Center" Margin="0,0,0,8" />
            <TextBlock Text="{Binding Count}" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,20" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="-" Width="60" Height="40" FontSize="20" Margin="4" Command="{Binding DecreaseCommand}" />
                <Button Content="+" Width="60" Height="40" FontSize="20" Margin="4" Command="{Binding IncreaseCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
